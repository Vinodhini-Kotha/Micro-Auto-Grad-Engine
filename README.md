# Micro-Auto-Grad-Engine
A vectorized autograd engine and MLP built from scratch in NumPy. Uses topological sorting to order backpropagation, processing multi-dimensional tensors and batch operations in parallel via matrix calculus.
